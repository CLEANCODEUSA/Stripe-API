# Exclude the directory that contains the project's dependencies
/vendor/

# Exclude the Composer executable itself, if it is placed in the project root
composer.phar

# Commit your application's lock file
# This ensures that everyone working on the project uses the exact same version of dependencies
# For more information, see the Composer documentation on commit best practices:
# https://getcomposer.org/doc/01-basic-usage.md#commit-your-composer-lock-file-to-version-control
!/composer.lock

# If you are developing a library, you might choose to ignore the lock file,
# but for most applications, it should be committed.
# # composer.lock

.env